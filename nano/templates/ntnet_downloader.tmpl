<i>Это установщик приложений, пожалуйста выберите одно из приложений ниже.</i><hr>
{{if data.error}}
	<h2>Ошибка скачивания</h2>
	<div class="itemLabel">
		Информация:
	</div>
	<div class="itemContent">
		{{:data.error}}
	</div>
	<div class="itemLabel">
		Перезапустить программу:
	</div>
	<div class="itemContent">
		{{:helper.link("РЕСТАРТ", null, {'PRG_reseterror' : 1})}}
	</div>
	<hr>
{{/if}}

	<h2>{{:data.downloadname ? 'Скачиваем' : 'Ожидаем'}}</h2>
	<i>{{:data.downloadname ? 'Ждите...' : 'Ничего не делаем...'}}</i>
	<div class="item">
		<div class="itemLabel">
			Имя:
		</div>
		<div class="itemContent">
			{{:data.downloadname ? data.downloadname : 'N/A'}}
		</div>
		<div class="itemLabel">
			Описание:
		</div>
		<div class="itemContent">
			{{:data.downloadname ? data.downloaddesc : 'N/A'}}
		</div>
		<div class="itemLabel">
			Размер:
		</div>
		<div class="itemContent">
			{{:data.downloadname ? (data.downloadcompletion + 'GQ / ' + data.downloadsize + 'GQ') : 'N/A'}}
		</div>
		<div class="itemLabel">
			Скорость:
		</div>
		<div class="itemContent">
			{{:data.downloadname ? data.downloadspeed : '0'}} GQ/s
		</div>
		<div class="itemLabel">
			Прогресс:
		</div>
		<div class="itemContent">
			{{:helper.displayBar(data.downloadcompletion, 0, data.downloadname ? data.downloadsize : 0, 'good')}}
		</div>
	</div>

	<br><hr>

	<h2>Очередь Загрузок</h2>
	<div class="item">
	{{for data.downloads_queue}}
		<div class="itemLabel">
			{{:index + 1}}:
		</div>
		<div class="itemContent">
			{{:value}}
			{{:helper.link('', 'close', {'PRG_removequeued' : value})}}
		</div>
	{{empty}}
		<i>Пусто.</i>
	{{/for}}
	</div>
	<br><hr>

	<h2>Внутренняя Память Терминала</h2>
	<div class="itemLabel">
		Диск:
	</div>
	<div class="itemContent">
		{{:helper.displayBar(data.disk_used, 0, data.disk_size, 'good')}}
		{{:data.disk_used}}GQ / {{:data.disk_size}}GQ
	</div>
	<table>
	{{for data.downloadable_programs}}
		<tr><td colspan=4><hr><center><h2>{{:value.category}}</h2></center>
		{{for value.programs :inner:innerKey}}
			<tr><td>{{:helper.link('', inner.icon, {'PRG_downloadfile' : inner.filename})}}
				<td><span class='white'>{{:inner.filedesc}}</span>
				<td>{{:inner.fileinfo}}
				<td>{{:inner.size}} GQ
			</div>
		{{/for}}
	{{/for}}
	</table>
	{{if data.hackedavailable}}
		<h2>Неопознаный внешний репозиторий</h2>
		<i>Данные файлы не сертифицированны NanoTrasen, используйте их на своё усмотрение.</i>
		<table>
		{{for data.hacked_programs}}
			<tr><td>{{:helper.link('', value.icon, {'PRG_downloadfile' : value.filename})}}
				<td><span class='white'>{{:value.filedesc}}</span>
				<td>{{:value.fileinfo}}
				<td>{{:value.size}} GQ
		</div>
		{{/for}}
		</table>
	{{/if}}
